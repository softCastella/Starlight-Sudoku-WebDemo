# 2026-09-10 WebDemo 리포 분리

앱 리포 `softCastella/Starlight-Sudoku`와 웹 데모 리포 `softCastella/Starlight-Sudoku-WebDemo`를 나눈다.

## 결정

| 항목 | 내용 |
| --- | --- |
| 새 리포 | `Starlight-Sudoku-WebDemo` (공개) |
| 시드 | 기존 `pages` 팁 `10d9b13` 전체 Flutter 트리 |
| 기본 브랜치 | `main` (Pages도 이 브랜치 푸시로 배포) |
| 공개 URL | `https://softcastella.github.io/Starlight-Sudoku-WebDemo/` |
| base-href | `/Starlight-Sudoku-WebDemo/` |
| 랜딩 CTA | 상대경로 `../?lang=` 유지(같은 Pages 안 게임). 절대 URL로 바꾸지 않음 |
| 커스텀 도메인 | 이번엔 github.io만. `starlight-sudoku.tycheworks.com`은 추후 |
| 앱 리포 Pages | 문서·README는 새 URL. 옛 `pages` 자동 배포는 중지 |

## 이전 체크리스트 (실행 전 확인)

- [x] `pages` HEAD = `10d9b13`
- [x] 워크플로: `.github/workflows/deploy-pages.yml` (`WEB_DEMO=true`, base-href)
- [x] 랜딩 CTA: `web/landing/index.html` → `../?lang=`
- [x] 방침 URL은 앱·웹 공통 `spark.tycheworks.com/.../privacy/` (변경 없음)
- [x] 형제 폴더 `Starlight-Sudoku-WebDemo` 없음
- [ ] 새 리포 생성
- [ ] pages 클론 → 설정 수정 → push
- [ ] WebDemo Pages(Actions) 활성화
- [ ] Actions 빌드 성공 + 공개 URL 200
- [ ] 앱 리포 README·하네스·배포 문서 링크 갱신
- [ ] 앱 리포 `pages` 워크플로 배포 중지
- [ ] 완료 검증 보고

## 새 리포에서 고칠 파일

1. `.github/workflows/deploy-pages.yml` — `branches: [main]`, base-href `/Starlight-Sudoku-WebDemo/`
2. `README.md` — WebDemo 전용 설명·URL
3. `Docs/Deployment/2026-09-01_GitHub_Pages_공개배포.md` — 저장소·URL·브랜치
4. `Docs/Harness/검증.md` — 공개 웹 URL·브랜치 규칙
5. `test/web_locale_test.dart` — 예시 path를 WebDemo로 (선택, 동작엔 무관)

## 앱 리포에서 고칠 파일

1. `README.md` — 웹 플레이·배포 안내 → WebDemo 리포
2. `Docs/Harness/검증.md` — 동일
3. `Docs/Deployment/2026-09-01_GitHub_Pages_공개배포.md` — 이전됨 안내
4. `.github/workflows/deploy-pages.yml` — 배포 비활성(또는 삭제) + 안내 주석

## 검증

1. WebDemo Actions `Deploy Flutter web to GitHub Pages` 성공
2. `https://softcastella.github.io/Starlight-Sudoku-WebDemo/` 로드
3. `/landing/` CTA → 같은 오리진 게임 `?lang=`
4. 앱 리포 README 링크가 새 URL
5. 앱 리포 `pages` 푸시로 옛 URL이 더 이상 갱신되지 않음
